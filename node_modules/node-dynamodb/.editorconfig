# top-most EditorConfig file
root = true

# Enforce unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true

# When editing JS files use space indentation (two spaces)
[*.js]
indent_style = space
index_size = 2
trim_trailing_whitespace = true
